package s25.cs151.application.views.constants;

/**
 * Enum representing the result of a form submission or data persistence operation.
 * Consists of either saved, edited, or duplicate.
 */
public enum SaveResult {
    SAVED,
    EDITED,
    DUPLICATE
}
